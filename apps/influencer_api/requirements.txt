fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.4
python-dotenv==1.0.1
redis==5.0.4
rq==1.15.1
httpx==0.27.0
requests==2.32.3
# NOTE: Model/image libs are intentionally omitted for now to avoid build issues on Python 3.13.
# We will add them when moving to a GPU-capable host or after pinning a supported Python runtime.
